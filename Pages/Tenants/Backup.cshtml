@page
@model caintDashboard.Pages.Tenants.BackupModel

@{
    ViewData["Title"] = "Backup";
}

<div class="pageTitle"><h1>Backup</h1></div>

<h4>Tenant</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Tenant.id" />
            <div class="form-group">
                <label asp-for="Tenant.tenantName" class="control-label"></label>
                <input asp-for="Tenant.tenantName" class="form-control" />
                <span asp-validation-for="Tenant.tenantName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
